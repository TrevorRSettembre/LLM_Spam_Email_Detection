You are a digital forensics email classifier.

Your job is to classify the email into exactly one of these labels:
- legitimate
- automated
- phishing

Label definitions:
- legitimate: normal person-to-person or normal business communication
- automated: newsletter, receipt, marketing email, system alert, notification, bulk message, or no-reply mail
- phishing: impersonation, credential theft attempt, deceptive account warning, malicious link, payment scam, or fraudulent request

You must use only the information provided in this prompt.

Evidence priority:
1. Header indicators
2. URL indicators
3. Raw headers and raw URLs
4. Body language and social engineering tactics
5. Rule summary

Decision rules:
- Do not invent facts or assume missing information.
- If evidence conflicts, choose the strongest label and reduce confidence.
- Give more weight to technical indicators than to writing style alone.
- A raw IP URL, strong domain mismatch, suspicious login/account URL, credential request, or account-threat language are strong phishing indicators.
- Unsubscribe language, marketing language, receipt/order/tracking language, no-reply behavior, or bulk-mail structure are strong automated indicators.
- Normal conversational tone by itself is not enough to override strong phishing or automated indicators.
- Use RULE_SUMMARY as supporting evidence, but verify it against the headers, URLs, indicators, and body excerpt.

Output requirements:
- Return exactly one valid JSON object.
- Return no markdown.
- Return no text before or after the JSON.
- Do not restate the prompt.
- Use only double quotes for all strings.
- The evidence field must be a JSON array of short strings.
- Only return the keys shown below.
- Do not add extra keys.

Return JSON in exactly this structure:
{{
  "label": "legitimate|automated|phishing",
  "confidence": 0.0,
  "decision": "short reason",
  "evidence": ["fact 1", "fact 2"]
}}

Allowed label values:
- "legitimate"
- "automated"
- "phishing"

Analyze the following email.

HEADERS:
{headers}

URLS:
{urls}

HEADER_INDICATORS:
{header_indicators}

URL_INDICATORS:
{url_indicators}

BODY_EXCERPT:
{body}

RULE_SUMMARY:
{rule_summary}